# TerraLeb assisted production setup progress

Last updated: 2026-09-05

This is the live owner-assistance record. Codex performs repository and browser work where safe, requests only one small owner action at a time, and verifies each result before advancing. No password, payment-card value, access token, private key, recovery code, or two-factor code belongs in this file or in chat.

## Current stop

- State: **paused until the owner says `continue OCI setup`** while accurate individual address information is obtained. No OCI tenancy has been reported as provisioned, no agreement has been accepted, and no trial has been started.
- Resume phrase: `continue OCI setup`.
- Next assisted action after resuming: complete only the OCI **Address Information** fields privately using accurate billing/contact information. Use the real street address in Address Line 1; leave Lines 2/3 blank unless genuinely needed; enter the real city; use the real governorate in Province if applicable; leave optional postal code blank if none is used. Do not enter a phone number or continue yet, and do not send the address in chat.
- Google Cloud/Firebase state: the owner received a 30-day trial-expiry reminder. Do **not** upgrade, close billing, unlink billing, or delete resources until Codex assists with a read-only inventory of the Firebase plan, linked projects, enabled services, resources and Earth Engine dependencies. FCM itself is a no-cost Firebase product; Google Cloud trial resources are a separate concern.
- Browser assistance note: Codex attempted the installed in-app browser connector, but its current client references a missing older cached support module. No browser page or external state was opened/changed. Until the connector is repaired, verification will use one-at-a-time visible page details or redacted screenshots rather than assuming success.
- Production authorization: blocked.
- Paid purchase/public deployment/domain or DNS change performed: none.

## Completed and verified

| Date | Work | Evidence | Cost/change | Rollback |
|---|---|---|---|---|
| 2026-08-26 | Preserved the reviewed dirty worktree and recorded the Phase-0 baseline. | `docs/predeployment/PHASE_0_REAUDIT_AND_IMPLEMENTATION_MAP.md` | No external cost or state change. | Documentation-only; retain as audit evidence. |
| 2026-08-26 | Upgraded the legal gate to evidence-backed `approved`, `blocked`, and narrowly allowed `not_in_release_scope` states. | `node --test scripts/verify-legal-readiness.test.js` — 6/6 passed. | No external cost. Production remains blocked. | Revert only with a reviewed replacement; never weaken the gate. |
| 2026-08-26 | Completed static production configuration and Docker secret-file validation. | `node scripts/verify-production-config.js` — passed. | No external cost. | Previous deployment configuration remains available in Git history after review/commit. |
| 2026-08-26 | Added and validated OCI Jeddah infrastructure code. | OpenTofu 1.12.6 checksum verified; `tofu fmt -check` and `tofu validate` passed. | No OCI resources created and no charge incurred. | No provider state exists to roll back. |
| 2026-08-26 | Added private OCI-compatible application storage and a separate encrypted database-backup service. | Storage/backup tests passed; production backup image built successfully. | No bucket, object, or cloud resource created. | Local driver remains the development default; production rollout is configuration-controlled. |
| 2026-08-26 | Hardened production online maps without removing Street, Hybrid, overlays, camera or drawing. | API map-provider tests passed; OpenAPI check passed; Flutter map tests and static analysis passed. | No ArcGIS account or paid usage created. | Protected-super-admin provider switch disables Hybrid and clients fall back to Street. |
| 2026-08-26 | Implemented the provider-neutral offline package boundary, authenticated resumable download, checksum/isolate extraction and deterministic package builder. | Migration full-chain passed; backend and Flutter offline tests passed; Python builder tests 2/2 passed. | No Copernicus/OSM source was downloaded and no package was published. | Last reviewed immutable package remains available; publication is versioned and reversible. |
| 2026-08-26 | Hardened the TerraLeb side of production AI integration and audited the real AI source at commit `ed06c63a231e8d3d711239acbfa65f017ca7ac07`. | Production configuration check passed; focused AI regression tests passed after allowlist compatibility corrections. | No GEE job, image push, provider purchase or production run occurred. | AI remains default-off per project; no image is production-authorized until the documented source contract passes. |
| 2026-08-26 | Reconciled reviewed account deletion with the owner policy without rebuilding the existing workflow. | Migrations `0071`–`0072`; API privacy execution tests 5/5; Flutter protected-admin workspace tests 10/10; API/Flutter analysis passed. | No production deletion was enabled and no production data was changed. | Keep `ACCOUNT_DELETION_EXECUTION_ENABLED=false`; code rollback must preserve already-created tombstones and cleanup evidence. |
| 2026-08-26 | Ran the architecture gate and selected the documented OCI x86 fallback. | TerraLeb API image built and executed on `linux/arm64`; pinned production PostGIS digest returned `no match for platform in manifest`. IaC now defaults to `VM.Standard.E4.Flex` 4 OCPU/32 GB. | No OCI resource or charge was created. E4 costs more than A1; final price must be reviewed before apply. | A1 can be reconsidered only after a reviewed multi-arch PostGIS image and external AI image pass the full ARM64 stack, migration, backup and restore gates. |
| 2026-08-26 | Aligned production AI container architecture with the selected E4 x86 host. | `.env.prod.example`, `compose.prod.yml`, static production configuration check — passed. | No external cost or state change. | Revert only together with a reviewed full-stack ARM64 migration. |
| 2026-08-26 | Completed the local Flutter regression and build gates. | `flutter analyze` — no issues; `flutter test` — 424/424; web release validation/WASM dry run and Android debug validation APK — passed. | No store upload or signing action occurred. | Build outputs are validation artifacts only and are not production-authorized. |
| 2026-08-26 | Completed local API static, migration, security, performance, Docker and proxy gates. | Lint/typecheck/build/OpenAPI 182 operations, migrations through 0072, zero production dependency audit findings, local performance suites, worker restart, health/readiness and Nginx WebSocket upgrade — passed. | No production data, DNS or service changed. | Local containers remain development-only; staging proof is still required. |
| 2026-08-26 | Added release dependency licence evidence generation. | Generator tests 4/4; inventory contains 292 production npm packages and 136 Flutter packages with no missing inventory entries. | No licence was automatically approved. | Regenerate for every exact release; retain legal/security review as a release gate. |
| 2026-08-26 | Expanded the assisted Phase-8 register and engineering report. | `docs/legal/MANUAL_ACTIONS_REQUIRED_FOR_PHASE_8.md`, `docs/predeployment/PHASE_7_ENGINEERING_VERIFICATION.md`, `docs/predeployment/TERRALEB_PHASES_0_8_ENGINEERING_AND_RELEASE_REPORT.md`; config/readiness/release/license tests 20/20. | No external action or approval was inferred. | Update documents and evidence status after each verified assisted step. |
| 2026-08-26 | Completed the clean full API regression after confirming only one Jest runner used the isolated test database. | `npm run test:ci -- --silent --coverageReporters=json-summary` — 51 suites passed, one skipped; 376 tests passed, two skipped; zero failed in 581.48 s. | No production data or external service changed. | Retain the result with the exact release; rerun after implementation changes. |
| 2026-08-26 | Recorded a local post-suite Nginx health/readiness smoke baseline. | `/health` 100 samples p50/p95 5.12/15.88 ms; `/ready` 50 samples p50/p95 5.79/7.15 ms. | No external cost/state change; these are not OCI staging SLO evidence. | Rerun the same and workload samples in staging; do not compare unlike environments. |
| 2026-08-26 | Attempted to connect the installed browser-control surface for assisted OCI setup. | Setup failed before navigation because the active browser client referenced missing cached `26.818.61809/scripts/browser-service.mjs`; the installed complete bundle is `26.820.60940`. | No page opened, credential entered, purchase made or external state changed. | Repair/reinstall the browser plugin separately; manual assisted steps remain available without exposing secrets. |
| 2026-08-28 | Owner opened the Oracle Cloud landing page and reported that **Sign up** is available. | Owner response `continue — Sign up`; browser-side verification remains unavailable because of the recorded connector fault. | No cost, account creation, provider agreement, credential, payment or region selection yet. | Close the page; no external state requires rollback. |
| 2026-08-28 | Owner reached the OCI form showing Customer type, Cloud Account/Tenancy Name and Home Region, and reported having entered initial account information. | Owner-reported visible fields; no secret values recorded. | Form progress may exist in the browser, but the tenancy has not been verified or authorized. | Do not submit; close or revise the form if the legal owner/customer type is wrong. |
| 2026-08-28 | Owner decided that the OCI customer is an individual rather than a registered organization/institution. | Owner response `Individual`. | The cloud tenancy will be personally owned; this does not determine TerraLeb's legal operator/controller or authorize production. | The choice can be revised before form submission; after account creation, ownership changes may require Oracle support/contracting. |
| 2026-08-28 | Owner confirmed selection of **Individual** on the OCI registration form. | Owner response `continue`; browser-side verification remains owner-observed because of the connector fault. | No charge, submission, tenancy or region selection yet. | Revise the selection before form submission if ownership facts change. |
| 2026-08-28 | Owner confirmed replacement of `ncrsadmin` with the neutral tenancy name `terralebprod`; no validation error was reported. | Owner response `continue`; exact availability remains finally confirmed only when Oracle accepts the form. | No charge or tenancy creation yet. | Change the name before submission if required; after provisioning it becomes part of account identification and service URLs. |
| 2026-08-28 | Owner selected OCI home region **Saudi Arabia West (Jeddah)** / `me-jeddah-1`; no availability error was reported. | Owner response `continue`; browser-side verification remains owner-observed. | No charge or tenancy creation yet. The choice becomes consequential on submission. | Change the region before submission; do not create in another region as a workaround. |
| 2026-08-28 | Owner submitted the first form and reached **Address Information**, **Payment/Identity Verification — You will not be charged**, **Agreement**, and **Start my free trial**. | Owner-reported page labels; official Oracle free-tier instructions reviewed 2026-08-28. | Tenancy is not yet reported as provisioned. Oracle may place a small temporary card authorization hold; Oracle states no card charge occurs unless the account is later explicitly upgraded. | Stop before agreement/trial submission; close the flow if the owner chooses not to create the free account. |
| 2026-08-28 | Owner paused OCI registration before completing address, phone, identity/payment verification, agreement or trial activation. | Owner message stating the address information will be obtained later; resume phrase recorded as `continue OCI setup`. | No new account provisioning or charge was reported. | Resume the same form later; verify its visible state before entering any additional information. |
| 2026-08-28 | Triaged the Google Cloud trial-expiry notice without changing billing. | Official Google Cloud Free Program and Firebase pricing-plan documentation reviewed; current console state still requires assisted verification. | No upgrade or fixed Firebase fee was authorized. FCM is documented as no-cost; paid Google Cloud and Earth Engine use remain separate decisions. | Do not change billing until the linked services are inventoried; Spark can support FCM-only use, while paid Cloud services require an active billing account. |
| 2026-09-04 | Removed internal identifiers and system-only details from the requester-facing personal-data report and fixed project-name wrapping across affected collection/map/import/export displays. | API typecheck/lint passed; privacy execution 5/5 passed; Flutter analysis passed; map experience 21/21 and affected import/export UI 29/29 passed. | No external or production state changed. Personal-data report format advances to `terraleb-personal-data-v3`. | Application rollback is possible before release; retain the stronger export minimization unless an approved access-right requirement proves a specific excluded field necessary. |
| 2026-09-04 | Verified why the offline screen has no downloadable map and replaced the vague empty state with an accurate, retryable explanation. | Development DB: one inactive legacy Esri row and zero current packages; offline UI regression included in the 21/21 map tests. | No map source was downloaded, licensed, approved, or published. | Publish only a reviewed immutable package; never reactivate the unsupported legacy Esri bulk-cache row. |
| 2026-09-04 | Re-ran the broad application and legal-gate regression checks after the export and map UI changes. | API production TypeScript build passed; Flutter full suite 426/426 passed; legal evidence-model suite 6/6 passed. The live readiness command remains fail-closed with 42 evidence findings. | No draft policy was promoted, no approval was inferred, and no production state changed. | Supply and verify each applicable external evidence item before approving exact policy/build hashes; do not remove the draft warning by bypassing the gate. |
| 2026-09-05 | Adopted the owner's product-policy decisions for Lebanon scope, 18+, role model, retention, deletion, masked attribution, accepted GIS, photos/location/free text, 35-day backups, GIS publication, project-scoped AI and Arabic/English. | `TERRALEB_OWNER_POLICY_DECISION_RECORD_V1.md` SHA-256 `0804aa982a4bde3e5b8c6c7b9dedc7484b75c5d39b01f35bcd2f6d4bf62d6546`; retention matrix SHA-256 `4a3dbd7c19f2a032d5ec23538867297bc90eb942341c3e9502a683b3579eb203`; legal evidence tests 7/7 passed. | No legal operator, provider contract, counsel approval or production authorization was inferred. The live gate reduced from 42 to 30 honest blocked items. | Reopen an owner decision only through a versioned replacement record; any public policy change requires new exact-text approval. |
| 2026-09-05 | Replaced active legal-draft fill-in prompts with the adopted owner rules and explicit external-fact boundaries. | Legal catalog parser and API legal-document tests 5/5 passed; title/placeholder regression assertions added. | Drafts remain unavailable in production and no public legal page was promoted. | A later counsel-approved immutable version supersedes this owner draft; prior versions remain available as required. |
| 2026-09-05 | Resolved the September dependency and release-image failures without the incompatible Express 5 upgrade. | npm audit: zero; OSV: zero; Gitleaks: no findings in 251 commits; API/Prometheus/Alertmanager/Blackbox/Loki zero high/critical; Alloy zero unsuppressed high/critical with six exact medium records. | No external cost/state change. Security fixes preserve Express 4 behavior and update transitive `qs`, `fast-uri`, `xmldom`, OS packages and Go modules. | Revert only to another scanner-clean dependency/image set; never reintroduce vulnerable versions to satisfy a bot PR. |
| 2026-09-05 | Re-ran the authoritative local CI gates against an isolated clean PostGIS database. | 73 migrations applied; full API CI passed; performance: bbox 69 ms, export 482 ms, workload 3,769 ms, restart 989 ms; Flutter 426/426 and 52.25%; production runtime/config passed. | Disposable local CI services only; development/production data was untouched. | Temporary CI containers may be stopped; rerun these gates after any implementation change and again on the pushed commit. |

## Awaiting owner/provider/counsel evidence

The authoritative item-by-item instructions and status are maintained in `docs/legal/MANUAL_ACTIONS_REQUIRED_FOR_PHASE_8.md`. Items remain blocked until Codex verifies real evidence. This section is intentionally not a self-service checklist.

## Assisted-step protocol

1. Codex presents one action, its cost/effect, and the expected success evidence.
2. The owner completes only that action and replies `continue` without sharing secrets.
3. Codex verifies the provider page or performs a safe repository/API/log test.
4. A failed verification is corrected before advancing.
5. Paid, public, irreversible, production-data, legal-publication, domain, and DNS actions always require explicit approval immediately before execution.
