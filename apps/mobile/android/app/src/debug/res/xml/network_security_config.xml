<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may target the host via emulator loopback or a LAN IP on a real device. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
