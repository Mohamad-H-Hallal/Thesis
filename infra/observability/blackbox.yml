modules:
  https_internal:
    prober: http
    timeout: 10s
    http:
      method: GET
      preferred_ip_protocol: ip4
      fail_if_not_ssl: true
      valid_status_codes:
        - 200
      tls_config:
        insecure_skip_verify: true
